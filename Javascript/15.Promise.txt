The Promise object represents the eventual completion (or failure) of an asynchronous operation and its resulting value.

-- it is used to handle async operation

it returns an object with promise State and promise result , in promise result we get the data that is returned from the promise.

// solve inversion of control and callback Hell

const cart=["kurta","shoes","pant"];

api.createOrder(cart,function(){
  api.proceedToPayment(function(){
    api.showOrderSummary(function(){
      api.updateWalletBalance();
    });
  });
});

// this can solved using promise

const promise= fetch('http://api.apis.guru/v2/list.json');
console.log(promise);
{
  data://promise resolve data
  state:rosolve|rejected|fulfilled
}